# MEM-001：开发记录与验收证据分层

状态：ADOPTED_FOR_THIS_BUNDLE（本次增补的设计；不是产品阶段签收）
日期：2026-09-16。来源：用户确认加入长期开发与跨会话机制；不虚构组织审核人。

采用：短 AGENTS 入口、功能账本、实际任务覆盖层、按任务会话的交接、派生摘要、路径依赖辅助、实际离线测试报告。原产品文档与验收基线保留；运行证据单独归档。

未采用：把所有聊天贴进一个 AGENTS、为此引入向量数据库/第二个 Agent、每次无差别读取全库、所有线程抢写一个 HANDOFF、把验收模板全改 PASS。

原因：减少重复上下文与事实漂移，同时保留版本和证据。实现状态和验证状态分开；报告只证明某范围在记录环境中的离线结果。

代价：路径/依赖登记需要维护；脚本不能自动确认语义依赖和教学正确性；远程 CI、CODEOWNERS 与保护规则必须由管理员启用；工具不会代替人工签收。

回退：原 v1.3 交付正文逐字节保留；独立移除新增开发工具即可恢复旧入口，但不得删除已经产生的实际运行证据或回归测试。
