# 可复制给新会话的接手指令

继续任务 M2-05（替换为实际任务）。先读根 AGENTS.md，再运行 `python tools/project_memory.py context --task M2-05`；已有 main 且要比较分支时加 `--base main`。

核对当前代码版本、未提交修改、相关功能和交接证据。不要把原始任务模板的 NOT_STARTED 误当实际进度，也不要把旧报告当当前通过。只补读本任务和受影响功能必要的源码、契约和测试。

简述：已经实现且有何证据、尚未完成/证据过期、本次变化及旧功能风险、要跑哪些测试。之后实施已授权动作。结束前更新实际账本、交接和摘要；未执行或因凭据阻塞必须明确标出，不自动签收、推送或启动付费调用。
