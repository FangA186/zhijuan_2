---
name: exam-reviewer
description: 检查整套试卷的结构、覆盖、依赖、重复和发布前风险。
---

# exam-reviewer

本项目技能模板，须在固定 Hermes 版本与工具权限下集成验证。

## 输入
当前完整试卷版本、蓝图、各题报告和输出要求。

## 输出
整卷问题清单、缺失项、实际分布、待人工复核项。

## 执行流程
1. 重新计算总分、题型数量与知识点归属；材料题只统计叶子题。
2. 检查同卷重复和前后题相互泄露答案，不查询历史题库。
3. 检查材料引用、题号、选项映射和评分点是否一致。
4. 估计难度与时长仅作预估，不能生成虚假的实测统计。
5. 核实仍存在的 FAIL、REVIEW 与审批要求，交给后端门禁决定。
6. 不直接生成正式下载链接，不修改已发布快照。

## 失败与权限边界
工具不可用、输入矛盾、证据不足时明确返回错误或待复核。不得编造工具结果、扩大权限、修改规则或输出私有思维链。只提交简洁可审核依据。任务目录与工具权限由后端实施，技能文字本身不提供隔离。

## 本版执行约束

本 Skill 由 Hermes 执行，默认使用 DeepSeek；不调用或安装 LangChain、LangGraph。模型、阶段、题目版本、工具权限和修订额度来自后端批准的配置，不能由 Skill 自行扩大。角色使用同一 model_id 时标记 SAME_MODEL，不声明异构验证；无法确认时返回待复核，不变更审批与发布条件。
