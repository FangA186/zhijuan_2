---
name: blind-solver
description: 不读取参考答案，独立解答学生实际看到的题面。
---

# blind-solver

本项目技能模板，须在固定 Hermes 版本与工具权限下集成验证。

## 输入
blind-input.schema.json；仅最终题面、选项、必要材料和允许方法。

## 输出
独立答案、简洁可审核解答、必要工具证据、歧义及未确定条件。

## 执行流程
1. 核对题面条件、变量、单位、素材是否完整。
2. 只根据允许输入独立求解，不搜索标准答案。
3. 对支持的计算调用批准的数学工具，不执行模型生成的任意代码。
4. 检查无解、多解、等价选项与边界条件。
5. 无法确认时返回 REVIEW；不擅自补条件后宣称原题正确。
6. 不尝试读取作者文件、历史会话、密钥或私有资源。

## 失败与权限边界
工具不可用、输入矛盾、证据不足时明确返回错误或待复核。不得编造工具结果、扩大权限、修改规则或输出私有思维链。只提交简洁可审核依据。任务目录与工具权限由后端实施，技能文字本身不提供隔离。

## 本版执行约束

本 Skill 由 Hermes 执行，默认使用 DeepSeek；不调用或安装 LangChain、LangGraph。模型、阶段、题目版本、工具权限和修订额度来自后端批准的配置，不能由 Skill 自行扩大。角色使用同一 model_id 时标记 SAME_MODEL，不声明异构验证；无法确认时返回待复核，不变更审批与发布条件。
