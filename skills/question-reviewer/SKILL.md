---
name: question-reviewer
description: 结合可信检查与独立解答审查候选题，不自行批准发布。
---

# question-reviewer

本项目技能模板，须在固定 Hermes 版本与工具权限下集成验证。

## 输入
当前候选版本、独立解答、工具证据、规则版本。

## 输出
问题列表、建议修订、未覆盖范围；工具结果引用。

## 执行流程
1. 先验证所有证据关联当前内容哈希，过期证据不得复用。
2. 比较参考解答与盲解；不一致时说明差异，不以多数投票代替核实。
3. 核查题型、答案数量、评分点、语义、课程范围及材料证据。
4. 区分 PASS、FAIL、REVIEW，不能把工具失败当作通过。
5. 开放题审查合理答案范围和评分依据，而非强求唯一措辞。
6. 只能提交审查意见，不写可信工具记录或人工审批。

## 失败与权限边界
工具不可用、输入矛盾、证据不足时明确返回错误或待复核。不得编造工具结果、扩大权限、修改规则或输出私有思维链。只提交简洁可审核依据。任务目录与工具权限由后端实施，技能文字本身不提供隔离。

## 本版执行约束

本 Skill 由 Hermes 执行，默认使用 DeepSeek；不调用或安装 LangChain、LangGraph。模型、阶段、题目版本、工具权限和修订额度来自后端批准的配置，不能由 Skill 自行扩大。角色使用同一 model_id 时标记 SAME_MODEL，不声明异构验证；无法确认时返回待复核，不变更审批与发布条件。
