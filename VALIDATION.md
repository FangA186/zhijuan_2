# v1.4 本次验证记录

核查日期：2026-09-16。仅报告本次实际执行情况，不把历史计数当新成绩。

## 本地结果

- 最终完整运行 6 个已实现离线组，均 PASS。
- 全量单元测试 **113 项**，失败 0、错误 0、跳过 0、预期失败 0。
- 其中新增开发工具专项 **48 项**（已包含在 113 项内，不能相加）。原 65 项基线测试继续保留并运行。
- 工具记录严格证据检查通过，派生 current.md 与实际账本一致。
- 实测内容含临时 Git 仓库的暂存/未暂存/未跟踪/已提交变更与重命名；缺基准拒绝；父仓库不冒认；新增任务不改基线；日志被改、范围变动、循环依赖、假 DONE、空测试/skip、会话覆盖等负面情况。
- 29 处入口/细则本地链接存在；新增 HTML 的 13 个导航锚点有效。
- 新增 HTML 用系统 Chromium set_content 渲染检查，1440px/390px 视口均无页面横向溢出；已查看桌面/手机截图。默认 Playwright 浏览器未安装、file:// 被环境策略拦截，故使用现有系统 Chromium 读取同一 HTML 内容，未修改环境策略。此项不等于试卷渲染或原 Word 重渲染。

完整结果：`acceptance-runs/devtools/v1_4-final/report.json` 及同目录的测试计数、脱敏日志。实测 Python 3.13.5/Linux，依赖版本见报告；CI 配置目标 Python 3.12 尚未在远程执行。

## 中断记录

前两次整体运行被外层工具的执行时限中断，报告 `v1_4-offline/`、`v1_4-verified/` 保留为 INCOMPLETE，未计为通过。单独诊断的 48 项专项通过；之后最终全套完整结束。没有删除失败用例或把 NOT_RUN 改为 PASS。

## 当前没有验证或交付的事项

未构建/启动应用、安装或克隆 Hermes、调用 DeepSeek、执行真实数据库迁移/渲染、评阅三学段题目、运行 APP/LIVE 测试，未启用远程 GitHub Actions、分支保护或 CODEOWNERS。14 项产品功能仍 NOT_STARTED；47 类运行验收仍 NOT_RUN。10 项应用回归行为只是映射，真实应用测试尚未实现。

当前可运行的是开发支持工具及原有离线参考检查。报告指纹只能核对字节范围和日志完整性，不能证明来源身份或测试覆盖完备。功能与任务记录需要开发者持续维护；实际应用测试与 CI 必须随开发补上。

## 文档和打包

原产品 v1.3 Word/HTML/Markdown 按字节保留，本轮没有重写或宣称更新其正文。新增 13 节长期开发手册为 Markdown + 独立 HTML；AGENTS 改为短入口，原开发细则按需阅读。ZIP 文件与 MANIFEST.sha256 全量核对，清单不含自身，不包含缓存、环境目录或字体。

## 历史核查（原文保留）

# v1.3.1 增补实际验证记录

日期：2026-09-16。本次只新增编程助手指导文件及包入口信息，没有实施应用或修改方案正文。

## 本轮实际完成

- 从 v1.3 ZIP 实际解包核对目录、任务依赖、命令、契约和已有验证器；新增根目录 AGENTS.md。
- AGENTS.md 中 22 处本地 Markdown 链接、21 个唯一目标均存在；代码围栏闭合，阶段编号与入口存在，没有同时创建大小写重名文件。
- 重新运行 `python -m unittest discover -s tests -v`：65 项通过；不是新增 65 项测试。
- 重新运行 `python verify_bundle.py`、`python verify_acceptance_assets.py`、`python tools/verify_execution_plan.py`：均退出 0。范围仍是参考规则与材料一致性，不是完整规范认证或真实验收。
- 独立 AGENTS.md 与 ZIP 根目录副本相同；重新生成并核对包内 MANIFEST.sha256 与 ZIP CRC。
- 原 v1.3 DOCX、HTML、Markdown 的 SHA-256 与原交接包一致。本轮没有修改或重新进行这些文件的排版/浏览器检查。

环境：Python 3.13.5，jsonschema 4.26.0，PyYAML 6.0.3。这不是 Python 3.12 产品部署兼容性证明。

详细命令与真实输出见 `tests/offline-validation-v1_3_1.log`，结构化摘要见同名 JSON。文件完整性复核记录见 `tests/package-integrity-v1_3_1.json`；该记录也纳入最终清单。

## 仍未执行

应用编译/启动、Hermes 克隆/安装、DeepSeek 调用、数据库迁移、真实题目生成或导出、安全隔离、质量、负载和恢复验收均未执行。32 项原始任务保持 NOT_STARTED；47 类运行用例保持 NOT_RUN。本文是开发约定，不是运行时安全控制或自动签收实现。

---

## 以下为 v1.3 历史验证记录（未在本次重新渲染文档）

# v1.3 本次实际验证记录

日期：2026-09-16。验证对象为**文档、执行与验收材料、参考规则**，不是应用或出题效果。

## 已执行

- `python -m unittest discover -s tests -v`：65 项测试通过。包含原有参考规则测试及新增依赖/映射/负面材料测试。
- `python verify_bundle.py`：5 份 JSON Schema、本地引用、25 个核心操作及 YAML 检查通过。不是完整 OpenAPI 规范认证。
- `python verify_acceptance_assets.py`：47 类用例和三学段输入材料检查通过；不执行 G-MIN/G-FULL。
- `python tools/verify_execution_plan.py`：32 项任务的唯一性、依赖无环、阶段先后、输入文件、任务与 25 个 API/47 类用例映射检查通过。
- 32 张任务卡的标题、实施步骤和目标文件均在 DOCX 与 HTML 正文中核对存在；第 08 分主题手册完整包含在 Markdown 总文档中。
- Word 渲染为 70 页并检查页面；无检测到的页面越界文本。修复了列表连续编号，任务步骤从 1 重新开始；目录链接可定位章节。
- HTML 使用原文件内容在 Chromium 中检查桌面 1440×1000 和手机 390×844；章节目标存在、无页面横向溢出、无脚本异常，并测试 M1 和命令入口跳转。

HTML 本地 file:// 导航被本次工具环境策略拒绝，改用同一文件字节的 set_content 渲染完成上述检查；不把工具环境限制误报成文件损坏，也不宣称已验证所有浏览器。PDF 为 Word 渲染检查的内部产物，不放入交付包。

## 实际离线环境

Python 3.13.5；库版本：{"jsonschema": "4.26.0", "PyYAML": "6.0.3", "python-docx": "1.2.0", "playwright": "1.57.0"}。这些是本次材料测试环境，**不是 Python 3.12 生产运行组合已经通过验证的证据**。

## 仍未执行

应用前后端实现/启动、Hermes 克隆或安装、真实 DeepSeek 调用、数据库迁移、真实题目质量评估、实际试卷生产导出、安全/隔离渗透测试、负载或恢复演练。

32 项实施任务保持 NOT_STARTED；47 类运行验收保持 NOT_RUN。G-PREP/G-BOOT/G-MIN/G-FULL 均未签收。签名、运行证据、费用和质量成绩均不得用此离线报告替代。

原始本次输出见 `tests/offline-validation-v1_3.log`，摘要见同名 JSON。历史测试数量只可在 CHANGELOG 的历史记录中作为历史保留，不代表本轮结果。
